services:
  - type: web
    name: finance-manager
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: Dockerfile
    dockerContext: .
    healthCheckPath: /api/transactions # A basic path to verify the app is up